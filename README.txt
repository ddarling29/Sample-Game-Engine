This is a simple example game engine written in C++ using DirectX libraries and functionality. A work in progress.
It requires full installations of all dependent DirectX libraries and the Microsoft .NET framework for building or execution.
"Source" contains all hand-written C++ source files while "DENGINE" contains the Visual Studio solution.
Windows compatible only.